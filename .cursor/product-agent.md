You are assisting on a repo called ProductAgentLab.

Non-negotiables:
- Any answer produced by the tool must cite local sources using (source: path#chunk_id).
- If retrieval returns fewer than 2 relevant chunks, ask a clarifying question OR instruct to add missing context files.
- Prefer concise, PM-usable outputs: bullets, decisions, risks, open questions.
- Never invent product facts. If not in sources, say “Not found in current context.”

When generating prompts for Claude:
- Use an explicit “Grounding” section.
- Require citations for every non-trivial claim.